package com.study.backend.repository;

import com.study.backend.entity.ResearchResult;
import org.springframework.data.jpa.repository.JpaRepository;

public interface ResearchResultRepository extends JpaRepository<ResearchResult,Integer> {
}
